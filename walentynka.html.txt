<!DOCTYPE html>
<html lang="pl">
<head>
<meta charset="UTF-8">
<title>💖 Walentynka 💖</title>

<style>
body {
    margin: 0;
    height: 100vh;
    background: linear-gradient(135deg, #ff9a9e, #fad0c4);
    font-family: Arial, sans-serif;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    overflow: hidden;
    text-align: center;
}

h1 {
    font-size: clamp(28px, 5vw, 42px);
    color: #b30000;
    animation: pulse 2s infinite;
}

@keyframes pulse {
    0% { transform: scale(1); }
    50% { transform: scale(1.05); }
    100% { transform: scale(1); }
}

.buttons {
    position: relative;
    z-index: 5;
}

button {
    padding: 20px 40px;
    font-size: 24px;
    border: none;
    border-radius: 15px;
    cursor: pointer;
    margin: 10px;
}

#yes {
    background: red;
    color: white;
    transition: transform 0.3s;
}

#no {
    background: #555;
    color: white;
    position: absolute;
}

#comments {
    margin-top: 20px;
    font-size: 20px;
    color: darkred;
    min-height: 30px;
}

/* Licznik */
#counter {
    margin-top: 20px;
    font-size: 18px;
    color: #800000;
}

/* Ekran KOCHAM CIĘ */
#loveScreen {
    position: fixed;
    inset: 0;
    background: red;
    color: white;
    display: none;
    justify-content: center;
    align-items: center;
    font-size: clamp(40px, 8vw, 80px);
    z-index: 20;
}

/* Serduszka */
.heart {
    position: fixed;
    font-size: 24px;
    animation: fly 1.5s forwards;
    pointer-events: none;
}

@keyframes fly {
    from { transform: translateY(0) scale(1); opacity: 1; }
    to { transform: translateY(-150px) scale(2); opacity: 0; }
}
</style>
</head>

<body>

<h1>Czy zostaniesz moją walentynką? ❤️</h1>

<div class="buttons">
    <button id="yes">TAK</button>
    <button id="no">NIE</button>
</div>

<div id="comments"></div>
<div id="counter"></div>

<div id="loveScreen">Kocham cię ;* ❤️</div>

<script>
const yes = document.getElementById("yes");
const no = document.getElementById("no");
const comments = document.getElementById("comments");
const loveScreen = document.getElementById("loveScreen");
const counter = document.getElementById("counter");

const texts = [
    "czy jesteś pewna?",
    "bądź moją walentynką no",
    "PAULINA!!!",
    "nie masz wyjścia ;*"
];

let commentIndex = 0;
let easterClicks = 0;

/* UCIEKAJĄCY PRZYCISK NIE */
no.addEventListener("mouseover", () => {
    const x = Math.random() * (window.innerWidth - 150);
    const y = Math.random() * (window.innerHeight - 80);
    no.style.left = x + "px";
    no.style.top = y + "px";

    comments.innerText = texts[commentIndex];
    commentIndex = (commentIndex + 1) % texts.length;
});

/* SERDUSZKA PO KLIKNIĘCIU */
document.addEventListener("click", (e) => {
    for (let i = 0; i < 5; i++) {
        const heart = document.createElement("div");
        heart.className = "heart";
        heart.innerText = "❤️";
        heart.style.left = e.clientX + Math.random()*20 - 10 + "px";
        heart.style.top = e.clientY + "px";
        document.body.appendChild(heart);

        setTimeout(() => heart.remove(), 1500);
    }

    easterClicks++;
    if (easterClicks === 15) {
        alert("💌 Tajna wiadomość: Jesteś najlepszą rzeczą, jaka mi się przydarzyła.");
    }
});

/* KOCHAM CIĘ */
yes.addEventListener("click", () => {
    loveScreen.style.display = "flex";
});

/* LICZNIK CZASU RAZEM */
const startDate = new Date("2024-02-14");

function updateCounter() {
    const now = new Date();
    const diff = now - startDate;

    const days = Math.floor(diff / (1000 * 60 * 60 * 24));
    counter.innerText = `Jesteśmy razem od ${days} dni ❤️`;
}

updateCounter();
setInterval(updateCounter, 60000);
</script>

</body>
</html>


