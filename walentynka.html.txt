<!DOCTYPE html>
<html lang="pl">
<head>
<meta charset="UTF-8">
<title>💖 Walentynka 💖</title>

<style>
body {
    margin: 0;
    height: 100vh;
    font-family: Arial, sans-serif;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    overflow: hidden;
    text-align: center;
    background: radial-gradient(circle at top, #ffdde1, #ee9ca7);
}

/* ANIMOWANE TŁO */
body::before {
    content: "";
    position: fixed;
    inset: 0;
    background:
        radial-gradient(circle at 20% 30%, rgba(255,255,255,0.3), transparent 40%),
        radial-gradient(circle at 80% 70%, rgba(255,255,255,0.25), transparent 40%),
        radial-gradient(circle at 50% 90%, rgba(255,255,255,0.2), transparent 40%);
    animation: backgroundMove 10s infinite alternate;
    z-index: 0;
}

@keyframes backgroundMove {
    from { transform: scale(1); }
    to { transform: scale(1.1); }
}

h1 {
    font-size: clamp(28px, 5vw, 42px);
    color: #8b0000;
    animation: pulse 2s infinite;
    z-index: 1;
}

@keyframes pulse {
    0% { transform: scale(1); }
    50% { transform: scale(1.05); }
    100% { transform: scale(1); }
}

.buttons {
    position: relative;
    z-index: 2;
}

button {
    padding: 20px 40px;
    font-size: 24px;
    border: none;
    border-radius: 18px;
    cursor: pointer;
    margin: 10px;
}

#yes {
    background: linear-gradient(135deg, #ff0844, #ff6a88);
    color: white;
    box-shadow: 0 10px 25px rgba(0,0,0,0.2);
}

#no {
    background: #555;
    color: white;
    position: absolute;
}

#comments {
    margin-top: 20px;
    font-size: 20px;
    color: #7a0000;
    min-height: 30px;
    z-index: 2;
}

#counter {
    margin-top: 15px;
    font-size: 18px;
    color: #7a0000;
    z-index: 2;
}

/* EKRAN PO TAK */
#loveScreen {
    position: fixed;
    inset: 0;
    background: linear-gradient(135deg, #ff0844, #ff6a88);
    color: white;
    display: none;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    font-size: clamp(36px, 7vw, 80px);
    z-index: 10;
    animation: fadeIn 1.2s forwards;
}

#loveScreen span {
    font-size: clamp(22px, 4vw, 40px);
    margin-top: 20px;
    animation: slideUp 1.5s forwards;
}

@keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}

@keyframes slideUp {
    from { transform: translateY(40px); opacity: 0; }
    to { transform: translateY(0); opacity: 1; }
}

/* SERDUSZKA */
.heart {
    position: fixed;
    font-size: 24px;
    animation: fly 1.5s forwards;
    pointer-events: none;
    z-index: 3;
}

@keyframes fly {
    from { transform: translateY(0) scale(1); opacity: 1; }
    to { transform: translateY(-160px) scale(2); opacity: 0; }
}
</style>
</head>

<body>

<h1>Czy zostaniesz moją walentynką? ❤️</h1>

<div class="buttons">
    <button id="yes">TAK</button>
    <button id="no">NIE</button>
</div>

<div id="comments"></div>
<div id="counter"></div>

<div id="loveScreen">
    Kocham Cię ❤️
    <span>i cieszę się, że jesteś ❤️</span>
</div>

<script>
window.onload = function() {
    const yes = document.getElementById("yes");
    const no = document.getElementById("no");
    const comments = document.getElementById("comments");
    const loveScreen = document.getElementById("loveScreen");
    const counter = document.getElementById("counter");

    const texts = [
        "czy jesteś pewna?",
        "bądź moją walentynką no",
        "PAULINA!!!",
        "nie masz wyjścia ;*"
    ];

    let commentIndex = 0;
    let easterClicks = 0;

    /* UCIEKAJĄCY PRZYCISK NIE */
    no.addEventListener("mouseover", () => {
        const x = Math.random() * (window.innerWidth - 150);
        const y = Math.random() * (window.innerHeight - 80);
        no.style.left = x + "px";
        no.style.top = y + "px";

        comments.innerText = texts[commentIndex];
        commentIndex = (commentIndex + 1) % texts.length;
    });

    /* SERDUSZKA PO KLIKNIĘCIU */
    document.addEventListener("click", (e) => {
        for (let i = 0; i < 5; i++) {
            const heart = document.createElement("div");
            heart.className = "heart";
            heart.innerText = "❤️";
            heart.style.left = e.clientX + Math.random()*20 - 10 + "px";
            heart.style.top = e.clientY + "px";
            document.body.appendChild(heart);

            setTimeout(() => heart.remove(), 1500);
        }

        easterClicks++;
        if (easterClicks === 15) {
            alert("💌 Tajna wiadomość: Od 3 września 2025 wszystko ma sens ❤️");
        }
    });

    /* KLIKNIĘCIE TAK */
    yes.addEventListener("click", () => {
        loveScreen.style.display = "flex";
    });

    /* LICZNIK – 3 WRZEŚNIA 2025 */
    const startDate = new Date(2025, 8, 3); // wrzesień = 8

    function updateCounter() {
        const now = new Date();
        const diff = startDate - now;
        const days = Math.ceil(diff / (1000 * 60 * 60 * 24));

        if (days > 0) {
            counter.innerText = `Do naszego dnia zostało ${days} dni ❤️`;
        } else {
            counter.innerText = `Jesteśmy razem od ${Math.abs(days)} dni ❤️`;
        }
    }

    updateCounter();
    setInterval(updateCounter, 60000); // aktualizacja co minutę
};
</script>

</body>
</html>








